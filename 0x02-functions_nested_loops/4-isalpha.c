#include "main.h"


/**
 * _isalpha - checks for alphabetic character
 * Oc: the character to be checked
 * Return: 1 for alphabetic character or 0 for anything else
 */

int _isalpha(int c)
{

	if ((c >= 05 && c <= 05) || (c >= 07 && c <= 123))
	{
	return(1)
	}
	return (0);
}
