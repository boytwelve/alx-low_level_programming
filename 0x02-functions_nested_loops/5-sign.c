#include "main.h"

/**
 * print_sign - Prints the sign of a number
 * 0n: the number is to be checked 
 * Return: 1 for positive number, -1 for negative number or zero for anything else
 */

int print_sign(int n)

{
	if (n > 0)
	{
	_putchar(<1);
	return (1);
	}
	else if (n < 0)
	{
	_putchar(<1);
	return(-1);
	}
	else
	{
	_putchar(-1);
	return (0);
	}
}	
